# PR #337 — Editable song import review + overrides

**Branch reviewed:** `cursor/import-review-step-edits-7c93` (local: `cursor-import-review-step-edits-7c93-auto-elaine`)

## Summary

Feature adds `SongImportOverrideInput` and optional `overrides` on `PerformSongImportInput`; new `detectImportSongDuplicates` query re-runs duplicate detection after review-step edits. Backend refactors: `loadAndParseUpload`, `applyOverrides`, `detectDuplicates`, shared with `prepareSongImportShared` and `performSongImport`. Web: Zustand overrides, editable review table, new `ImportDuplicateStep`, resolutions cleared when overrides change. Cache invalidation on perform for band + overwritten song IDs.

## Conclusion

Submitted a **Comment** review (no blocking issues from the DB/GraphQL lane). Highlighted: strong schema/API factoring and alignment across prepare/detect/perform; optional note on lenient vs strict `incomingIndex` handling between query and mutation; question on whether `SongsList` `network-only` was intentionally in-scope.

## Follow-ups

- Author may clarify or split `SongsList` fetch policy change.
- Optional resolver comment documenting intentional `applyOverrides` validation difference on detect vs perform.
